import React from 'react';
import Logout from './../../../components/Logout';

const Dashboard = () => {
  return (
    <div>
      <h2>Dashboard</h2>
      <Logout />
    </div>
  );
};

export default Dashboard;
